# API Testing Environment Variables
# Copy this file to .http.env and update with your values

# Authentication token (get this from login response)
@token = YOUR_BEARER_TOKEN_HERE

# Reset password token (get this from email)
@reset_token = YOUR_RESET_TOKEN_HERE

# Base URL
@baseUrl = http://localhost:8000
